public class StartState : IStateManager
{
    public void OnEnter()
    {
        //UIManager.Instance.HidenAllPanel();
        //MapManager.Instance.RenderMap(LevelManager.Instance.Level);
        //Player.Instance.OnInit();
        //GameManager.Instance.ChangeState(new PlayState());
    }

    public void OnExit()
    {
    }
}
